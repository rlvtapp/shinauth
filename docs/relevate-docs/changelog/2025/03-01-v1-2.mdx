---
title: "1.2 Release"
description: "Stripe, Captcha, API Keys, Teams, Init CLI, and more."
type: improved
date: 2025-03-01
label: "v1.2"
tags: ["Release"]
---

# Shinauth 1.2 – Stripe, Captcha, API Keys, Teams, Init CLI, and more

To upgrade, run:

```bash
pnpm add shinauth@1.2
```

***

### **Stripe Plugin (Beta)**

Stripe integration for customer management, subscriptions, and webhooks.

```bash
pnpm add @shinauth/stripe
```

```ts title="auth.ts"
import { stripe } from "@shinauth/stripe";

export const auth = shinAuth({
  plugins: [
    stripe({
      createCustomerOnSignup: true,
      subscription: {
        enabled: true,
        plans: [
          {
            name: "pro",
            priceId: "price_1234567890",
          },
        ],
      },
    }),
  ],
});
```

Read the [Stripe Plugin docs](/plugins/stripe) for more information.

### **Captcha Plugin**

Protect your authentication flows with Google reCAPTCHA and Cloudflare Turnstile. Works for signup, signin, and password resets.

```ts title="auth.ts"
import { captcha } from "shinauth/plugins";

const auth = shinAuth({
  plugins: [
    captcha({
      provider: "cloudflare-turnstile", // or "google-recaptcha"
      secretKey: process.env.TURNSTILE_SECRET_KEY!,
    }),
  ],
});
```

Read the [Captcha Plugin docs](/plugins/captcha) for more information.

### **API Key Plugin**

Generate and manage API keys with rate limiting, expiration, and metadata. Supports session creation from API keys.

```ts title="auth.ts"
import { apiKey } from "shinauth/plugins";

const auth = shinAuth({
  plugins: [apiKey()],
});
```

Read the [API Key Plugin docs](/plugins/api-key) for more information.

### **Teams/Sub-Organizations**

Organizations can now have teams or sub-organizations under them.

```ts title="auth.ts"
const auth = shinAuth({
  plugins: [
    organization({
      teams: {
        enabled: true,
      },
    }),
  ],
});
```

Read the [Organization Plugin docs](/plugins/organization#teams) for more information.

### **Init CLI**

The CLI now includes an `init` command to add Shinauth to your project.

```bash title="terminal"
pnpm dlx @shinauth/cli init
```

### **Username**

* Added `displayName` for case-insensitive lookups while preserving original formatting.
* Built-in validation.


**Note:**

  If you're using the Username plugin, make sure to add the `displayName` field
  to your schema.


### **Organization**

* **Multiple Roles per User** – Assign more than one role to a user.

### **Admin Plugin**

* Manage roles and permissions within the admin plugin. [Learn more](/plugins/admin)
* `adminUserIds` option to grant specific users admin privileges. [Learn more](/plugins/admin#usage)

***

## 🎭 New Social Providers

* [TikTok](/authentication/tiktok)
* [Roblox](/authentication/roblox)
* [VK](/authentication/vk)

***

## ✨ Core Enhancements

* **Auto Cleanup** for expired verification data
* **Improved Google One Tap** integration with JWT verification and enhanced prompt handling
* **Phone-based Password Reset** functionality
* **Provider Control Options**:
  * Disable signups for specific providers
  * Disable implicit signups for specific providers
  * Control default scopes and allow custom scopes on request
* **Enhanced Database Hooks** with additional context information

***

## 🚀 Performance Boosts

We rewrote **better-call** (the core library behind Shinauth) to fix TypeScript editor lag. Your IDE should now feel much snappier when working with Shinauth.

***

## ⚡ CLI Enhancements

### **`init` Command**

The CLI now includes an `init` command to speed up setup:

* Scaffold new projects
* Generate schemas
* Run migrations

[Learn more](/concepts/cli)

***

## 🛠 Bug Fixes & Stability Improvements

A lot of fixes and refinements to make everything smoother, faster, and more reliable. Check out the [changelog](https://github.com/shinauth/shinauth/releases/tag/v1.2.0) for more details.

***

```bash
pnpm add shinauth@1.2.0
```

**Upgrade now and take advantage of these powerful new features!** 🚀
